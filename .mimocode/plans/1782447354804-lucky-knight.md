# Plan: Make pytest-llm Production-Ready

## Pre-requisite: Resolve Merge Conflicts

The repo has 3 unmerged files from a diverged branch: `pyproject.toml`, `README.md`, `.gitignore`. Both sides added different content. Since we're rewriting `pyproject.toml` and `README.md` completely, we'll resolve by keeping the pytest-llm side (HEAD) for all 3 files. The `app/` directory files from the other branch are part of a different project ("AI Reliability Platform") and should be cleaned up later — out of scope for this task.

**Action**: `git checkout --ours pyproject.toml README.md .gitignore` then `git add` them.

---

## Task 1: README Overwrite

**File**: `README.md` (full rewrite, <200 lines)

Structure per user spec:
1. **Header**: Badge row (Python 3.9+, PyPI, MIT, Tests), one-liner description, problem paragraph
2. **Quick Start**: `pip install pytest-llm` + test function showing `assert_faithful`, `assert_tone`, `assert_safe` (~10 lines)
3. **Architecture diagram**: ASCII showing pytest test → pytest-llm assertions → local path (sentence-transformers) / LLM judge path (OpenAI/Anthropic/Groq/Ollama)
4. **All 8 assertions table**: `assert_faithful`, `assert_no_hallucination`, `assert_tone`, `assert_semantic_similarity`, `assert_contains_claim`, `assert_safe`, `assert_language`, `assert_regression` — 3 columns: name, what it checks, uses API?
5. **Configuration**: env vars + conftest.py approach
6. **CI/CD**: GitHub Actions YAML inline
7. **Provider support table**: 4 rows
8. **Works with langgraph-replay**: paragraph + integration command

---

## Task 2: GitHub Actions Workflow + Dev Dependencies

**Files**:
- Create `.github/workflows/tests.yml` — matrix CI for Python 3.9-3.12, runs `pytest tests/ -v --tb=short`
- Edit `pyproject.toml` — add `[project.optional-dependencies] dev = ["pytest>=7.0", "pytest-cov"]` (merge conflict must be resolved first)

---

## Task 3: LLM Judge Caching

**Files**:
- Edit `src/pytest_llm/judge.py`:
  - Add `JudgeCache` class with `_key()`, `get()`, `set()` methods using SHA-256 hashing
  - Create session-level singleton `_cache_instance = JudgeCache()`
  - In `LLMJudge.judge()`: check cache before API call, store result after successful call
  - Cache key: `provider:model:system_prompt:user_prompt`

- Edit `tests/test_judge.py`:
  - Add `test_judge_cache_hit_avoids_api_call`: call `judge()` twice with identical prompts, mock `_call_llm`, assert it's called exactly once (second call hits cache)

---

## Task 4: PyPI Setup

**Files**:
- Edit `pyproject.toml` — add license, authors, keywords, classifiers, project.urls
- Create `LICENSE` — MIT license, year 2025, name Sushit

---

## Task 5: Verification

Run `pytest tests/ -v` and confirm all tests pass including the new cache test.

---

## Critical Files

| File | Action |
|------|--------|
| `pyproject.toml` | Resolve conflict + rewrite for pytest-llm + add dev deps + PyPI metadata |
| `README.md` | Resolve conflict + full rewrite |
| `.gitignore` | Resolve conflict (keep ours) |
| `.github/workflows/tests.yml` | Create new |
| `LICENSE` | Create new |
| `src/pytest_llm/judge.py` | Add JudgeCache + singleton + integration in judge() |
| `tests/test_judge.py` | Add cache hit test |
